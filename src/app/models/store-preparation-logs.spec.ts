import { StorePreparationLogs } from './store-preparation-logs';

describe('StorePreparationLogs', () => {
  it('should create an instance', () => {
    expect(new StorePreparationLogs()).toBeTruthy();
  });
});
