import { ReturnedPOTransactionStatus } from './returned-potransaction-status';

describe('ReturnedPOTransactionStatus', () => {
  it('should create an instance', () => {
    expect(new ReturnedPOTransactionStatus()).toBeTruthy();
  });
});
