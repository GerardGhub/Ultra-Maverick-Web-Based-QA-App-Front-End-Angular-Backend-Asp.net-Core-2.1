import { WhRejectionApproval } from './wh-rejection-approval';

describe('WhRejectionApproval', () => {
  it('should create an instance', () => {
    expect(new WhRejectionApproval()).toBeTruthy();
  });
});
