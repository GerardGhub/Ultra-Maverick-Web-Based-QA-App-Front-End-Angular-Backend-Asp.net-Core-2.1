import { SystemCapabilityStatus } from './system-capability-status';

describe('SystemCapabilityStatus', () => {
  it('should create an instance', () => {
    expect(new SystemCapabilityStatus()).toBeTruthy();
  });
});
