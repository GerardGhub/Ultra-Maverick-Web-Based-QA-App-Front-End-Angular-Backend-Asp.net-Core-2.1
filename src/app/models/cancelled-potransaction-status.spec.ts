import { CancelledPOTransactionStatus } from './cancelled-potransaction-status';

describe('CancelledPOTransactionStatus', () => {
  it('should create an instance', () => {
    expect(new CancelledPOTransactionStatus()).toBeTruthy();
  });
});
